const nombre = "Hello world"; //con "const" no podras cambiar el valor dde la variable, mientras que con "let" si puedes  

nombre = "hola mundou"; // No se cambiará el valor de la variable, ya que es una constante (const)

console.log(nombre);